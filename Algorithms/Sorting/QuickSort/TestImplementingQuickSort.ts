import { quickSort } from './ImplementQuickSort';

let numbers = [99, 44, 6, 2, 1, 5, 63, 87, 283, 4, 0];
console.log(quickSort(numbers));
numbers = [];
console.log(quickSort(numbers));
numbers = [1];
console.log(quickSort(numbers));